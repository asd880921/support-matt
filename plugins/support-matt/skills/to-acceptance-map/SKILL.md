---
name: to-acceptance-map
description: 整條 branch 的 ticket 全部開發完畢後，於獨立 session 盤點自動化測試對驗收項目的覆蓋情形，產出 acceptance-map.md。驗收來源有兩處：各 ticket 的驗收條件，以及 engineering-spec.md 的設計檢核點（含從未進入任何 ticket 的項目）。逐項判定已涵蓋／覆蓋不足／未涵蓋並標註對應測試。對 ticket、設計文件、功能程式碼與測試程式一律唯讀，只建立或覆寫 acceptance-map.md；不補寫測試、不修程式。當使用者在 branch 開發完畢、要做最終覆蓋盤點並取得可追溯的驗收項目對測試清單時使用。
---

# to-acceptance-map

整條 branch 的 ticket 全部完成後，盤點**自動化測試對驗收項目的覆蓋情形**，產出一份可追溯的對應清單。

## 為什麼要獨立執行

`implement-stepwise` 在每個 commit 完成時會記錄「本次寫了哪些測試、涵蓋哪幾條驗收條件」。**那份記錄是實作者的自我聲明，不是證據。**

本 skill 的價值來自獨立性：在**乾淨的新 session** 執行，不帶開發過程的 context，逐項自行確認測試確實存在、且真的涵蓋該項目。checklist 上的 `tests` 記錄只當**搜尋線索**使用——依它去定位測試很有效率，但**必須自行開啟測試確認內容**，不得直接採信。記錄與實際不符時，以實際為準，並在報告中指出落差。

## 唯讀規範（最高優先）

- **只建立或覆寫 `acceptance-map.md` 一個檔案。**
- 對 ticket 檔案、`engineering-spec.md`、`spec.md`、功能程式碼、測試程式**一律唯讀**。
- **不補寫任何測試、不修改任何程式碼、不修改設計文件、不勾選任何 checklist。**
- 發現缺漏時只回報，由使用者決定後續處理。

## 執行流程

### 1. 取得範圍

從使用者指定的 feature 目錄取得（路徑依 `.ai/docs/agents/issue-tracker.md` 的慣例，通常是 `.ai/.scratch/<feature>/`）：

- `issues/` 底下全部 ticket 檔案
- `engineering-spec.md`（若存在）

未指定時，依當前 git 分支名稱推斷對應的 feature 目錄；推斷不出來就停下來問，不要猜。

若仍有 ticket 的 Commit checklist 未全部勾完，**先回報並確認**是否仍要繼續盤點——對未完成的 branch 盤點，結果會大量出現「未涵蓋」而失去意義。

### 2. 匯集驗收項目

兩個來源都要納入：

- **各 ticket 的驗收條件** —— ticket 檔案中的 checkbox 項目。以 `<ticket 編號>-<項次>` 作為識別（例如 `01-08`）。
- **`engineering-spec.md` 的設計檢核點** —— DC 表格全部列，沿用其 `DC-xx` 編號。

**設計檢核點必須全部納入，包含從未被任何 ticket 吸收的項目。** 這正是本 skill 存在的理由之一：以否定式陳述表達的檢核點（「未使用 X」「未產生 Y」）在拆票時容易蒸發，因為它們演示不出來；feature 層級的盤點是它們唯一的回收點。

同理，**橫跨多張 ticket 的項目**（例如「查詢與匯出走同一個取資料的方法」）在任何單張 ticket 的收尾都驗不到，只有這裡驗得到。

### 3. 逐項比對測試

對每一個驗收項目，找出涵蓋它的自動化測試——單元測試、整合測試、UI/E2E 測試皆計入。

探索方式：若專案已建立程式碼知識圖譜或等效索引工具（讀 `CLAUDE.md` / `AGENTS.md` 的宣告，或檢查可用的 MCP 工具判斷），**優先用它定位測試與被測目標**，再針對性讀取；本 skill 不指定使用哪一套。這是一次 feature 層級的全面盤點，靠 grep 加整檔閱讀會迅速累積大量無用 context。

判定三分類：

| 判定 | 條件 |
| --- | --- |
| **已涵蓋** | 存在測試，且其斷言確實驗證該項目所述的行為或約束 |
| **覆蓋不足** | 有相關測試，但只驗到部分條件、只測快樂路徑、或斷言不足以判定該項目成立 |
| **未涵蓋** | 找不到任何驗證該項目的測試 |

判定原則：

- **以斷言為準，不以測試名稱為準。** 名稱寫得像、但斷言沒驗到的，是「覆蓋不足」。
- **不把「程式碼看起來有實作」當成已涵蓋。** 本 skill 盤點的是測試覆蓋，不是實作完成度。
- 部分設計檢核點是**結構性約束**（例如「四道判定全部在資料庫執行」「未使用含系統倉短路的授權判斷式」），自動化測試往往驗不到。這類項目照實判為「未涵蓋」，並在備註註明它本質上需要人工或靜態檢查，不要為了讓表格好看而勉強對應一個不相干的測試。

### 4. 寫出 `acceptance-map.md`

寫入 feature 目錄下的 `acceptance-map.md`（與 `issues/` 同層）。既有檔案直接覆寫——本 skill 可重複執行，每次都反映當下狀態。

```md
# 驗收覆蓋盤點：<feature>

- 分支：<branch-name>
- 盤點時間：<YYYY-MM-DD>
- 來源：issues/01..NN、engineering-spec.md 設計檢核點

## 摘要

| 判定 | 數量 |
| --- | --- |
| 已涵蓋 | N |
| 覆蓋不足 | N |
| 未涵蓋 | N |

## 驗收條件（來自 ticket）

| 編號 | 驗收項目 | 判定 | 對應測試 | 備註 |
| --- | --- | --- | --- | --- |
| 01-08 | <驗收條件原文> | 已涵蓋 | `TestXxx.某測試方法` | |
| 01-09 | <驗收條件原文> | 覆蓋不足 | `TestXxx.某測試方法` | 只驗了快樂路徑，邊界值未測 |

## 設計檢核點（來自 engineering-spec.md）

| 編號 | 檢核點 | 判定 | 對應測試 | 備註 |
| --- | --- | --- | --- | --- |
| DC-02 | <檢核點原文> | 未涵蓋 | — | 結構性約束，需人工或靜態檢查 |
| DC-05 | <檢核點原文> | 未涵蓋 | — | 未被任何 ticket 吸收 |

## 與 checklist 記錄的落差

| 位置 | checklist 記錄 | 實際 |
| --- | --- | --- |
| 01 commit 2 | `TestXxx.某測試` 涵蓋 #12 | 該測試存在，但斷言未涉及 #12 |

<沒有落差時寫「無落差」。>

## 待處理

- 依序列出「覆蓋不足」與「未涵蓋」的項目編號，供使用者決定補測試、補人工檢查，或判定為不適用。
```

### 5. 回報

呈現摘要數字與「待處理」清單，並明確告知使用者：本 skill **未修改任何程式或文件**，補測試需由使用者決定後另行處理。

## 執行限制

- 盤點的是**測試覆蓋**，不是功能是否完成，也不是程式碼品質。發現實作缺陷時可在備註指出，但不擴大成 code review。
- 驗收項目本身有問題時（語意矛盾、無法判定真偽），照實記錄為「無法判定」並說明，**不要自行改寫項目**，也不要回頭修設計文件。
- 不與 `engineering-spec.md` 定稿模式競爭：定稿由 `to-engineering-spec` 負責，它會在 DC 表補「驗證結果」欄。本檔案是它的輸入之一，不是它的替代品。

## 下一步引導（純提示，不主動調用）

- **有未涵蓋或覆蓋不足** → 提示使用者可回到 `implement-stepwise`，將補測試視為新的 commit item 處理。
- **全部收斂** → 提示使用者可執行 `to-engineering-spec` 的定稿模式，依本檔案逐條標註設計檢核點的驗證結果；需交付時再以 `engineering-spec-deliverable` 產出交付版。
